// NOTE: import error below is not present when using Volar's "takeover" mode
import VueComp from "./components/PrimTest/PrimTest.vue"

import { createApp } from "vue"
createApp(VueComp).mount("#vue")
