version: '3'

services:
  caddy:
    build:
      context: ./proxy
      target: prod
    restart: "always"

  postgres:
    restart: "always"

  backend:
    build:
      context: ./apps/backend
      target: prod
    restart: "always"

  frontend:
    build:
      context: ./apps/frontend
      target: prod
    restart: "always"
