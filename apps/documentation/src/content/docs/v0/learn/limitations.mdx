---
title: Limitations
---

# {frontmatter.title}

Prim+RPC allows functions on the server to be called from the client. This is possible because all function calls on the
client are captured on the client, processed on the server, and sent back to the client. But there are some limitations.
