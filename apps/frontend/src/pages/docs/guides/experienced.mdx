{/* SECTION: setup */}

import { LayoutDocs } from "@/components/LayoutDocs"
import { Alert } from "@/components/Alert"
export const meta = {
	title: "Experienced Guide",
}
export default ({ children }) => <LayoutDocs meta={meta}>{children}</LayoutDocs>

{/* !SECTION setup */}

# {meta.title}

<Alert icon="carbon:warning" type="alert-warning">

In this guide, we'll use additional tooling that is completely unrelated to Prim+RPC. You can use Prim+RPC with your own
tools.

</Alert>

...
